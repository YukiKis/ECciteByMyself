<table class="table">
  <thead><tr><th>ジャンル検索</th></tr></thead>
  <tbody>
    <% Category.all.each do |category| %>
      <tr><td><a href="#"><%= category.name %></a></td></tr>
    <% end %>
  </tbody>
</table>