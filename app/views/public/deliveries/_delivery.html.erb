<tr>
  <td style="line-height: 3;"><%= delivery.postcode %></td>
  <td style="line-height: 3;"><%= delivery.address %></td>
  <td style="line-height: 3;"><%= delivery.name %></td>
  <td style="line-height: 3; text-align: center">
    <%= link_to "編集する", edit_delivery_path(delivery), class: "btn btn-primary" %>
    <%= link_to "削除する", delivery_path(delivery), method: :delete, class: "btn btn-danger", remote: true %>
  </td>
</tr>